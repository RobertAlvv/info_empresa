import 'package:flutter/material.dart';

class InfoPrinter extends StatelessWidget {
  const InfoPrinter({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        appBar: AppBar(),
      ),
    );
  }
}