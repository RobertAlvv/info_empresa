import 'package:flutter/material.dart';

class InfoModem extends StatelessWidget {
  const InfoModem({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        appBar: AppBar(),
      ),
    );
  }
}
